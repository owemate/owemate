import { StatusBar } from 'expo-status-bar';
import * as Notifications from 'expo-notifications';
import { ActivityIndicator, BackHandler, KeyboardAvoidingView, Platform, Pressable, StyleSheet, Text, TextInput, View } from 'react-native';
import type { ReactNode } from 'react';
import { useEffect, useState } from 'react';
import { SafeAreaProvider, SafeAreaView } from 'react-native-safe-area-context';
import { AddTransactionScreen } from './src/screens/AddTransactionScreen'; import { AuthScreen } from './src/screens/AuthScreen'; import { DashboardScreen } from './src/screens/DashboardScreen'; import { PeopleScreen } from './src/screens/PeopleScreen'; import { PersonDetailsScreen } from './src/screens/PersonDetailsScreen'; import { ReminderSettingsScreen } from './src/screens/ReminderSettingsScreen'; import { SplashScreen } from './src/screens/SplashScreen'; import { WelcomeScreen } from './src/screens/WelcomeScreen'; import { CalendarScreen } from './src/screens/CalendarScreen'; import { NotificationsScreen } from './src/screens/NotificationsScreen'; import { ProfileScreen } from './src/screens/ProfileScreen'; import { useAppRootState } from './src/screens/AppRoot';
type AppScreen='welcome'|'signin'|'signup'|'forgotPassword'|'resetPassword'|'dashboard'|'add'|'people'|'personDetails'|'reminders'|'profile'|'calendar'|'notifications'; type Message={type:'error'|'success';text:string}|null; const displayName=(fullName:string,email:string)=>fullName.trim()||email.split('@')[0].replace(/[._-]+/g,' ').replace(/\b\w/g,c=>c.toUpperCase())||'there'; export default function App(){return <SafeAreaProvider><AppContent/></SafeAreaProvider>}
function AppContent(){const app=useAppRootState();const[personScreen,setPersonScreen]=useState<string|null>(null);const[pendingNotificationId,setPendingNotificationId]=useState<string|null>(null);const[showSplash,setShowSplash]=useState(true);useEffect(()=>{const t=setTimeout(()=>setShowSplash(false),1800);return()=>clearTimeout(t)},[]);useEffect(()=>{const handle=(r:Notifications.NotificationResponse)=>{const id=r.notification.request.content.data?.transactionId;if(typeof id==='string')setPendingNotificationId(id)};const s=Notifications.addNotificationResponseReceivedListener(handle);void Notifications.getLastNotificationResponseAsync().then(r=>{if(r)handle(r)});return()=>s.remove()},[]);useEffect(()=>{if(!pendingNotificationId||!app.transactions.length)return;const t=app.transactions.find(i=>i.id===pendingNotificationId);if(!t)return;setPersonScreen(t.person);app.clearMessage();app.setScreen('personDetails');setPendingNotificationId(null)},[pendingNotificationId,app.transactions]);useEffect(()=>{const h=()=>{const p:Partial<Record<AppScreen,AppScreen>>={signin:'welcome',signup:'welcome',forgotPassword:'signin',resetPassword:'signin',add:app.addReturnScreen==='personDetails'?'personDetails':'dashboard',people:'dashboard',personDetails:'people',reminders:'profile',profile:'dashboard',calendar:'dashboard',notifications:'dashboard'};const target=p[app.screen as AppScreen];if(!target)return false;app.clearMessage();app.setScreen(target);return true};const s=BackHandler.addEventListener('hardwareBackPress',h);return()=>s.remove()},[app.screen,app.addReturnScreen]);const home=()=>app.setScreen('dashboard'),transactions=()=>app.setScreen('people'),add=()=>app.startAddTransaction(),calendar=()=>app.setScreen('calendar'),notifications=()=>app.setScreen('notifications'),profile=()=>app.setScreen('profile');if(showSplash)return <AppShell><SplashScreen/></AppShell>;if(app.loading)return <AppShell><View style={styles.loading}><ActivityIndicator size="large" color="#0F766E"/><Text style={styles.loadingText}>Loading OweMate…</Text></View></AppShell>;if(app.screen==='welcome')return <AppShell><WelcomeScreen onSignIn={()=>app.setScreen('signin')} onSignUp={()=>app.setScreen('signup')}/></AppShell>;if(app.screen==='signin'||app.screen==='signup')return <AppShell><AuthScreen mode={app.screen} email={app.email} password={app.password} fullName={app.fullName} submitting={app.authSubmitting} configured={app.configured} message={app.message?.text??null} messageType={app.message?.type??'error'} onEmailChange={app.setEmail} onPasswordChange={app.setPassword} onFullNameChange={app.setFullName} onSubmit={()=>void app.handleAuth()} onForgotPassword={()=>app.setScreen('forgotPassword')} onSwitchMode={()=>{app.setPassword('');app.setFullName('');app.setScreen(app.screen==='signin'?'signup':'signin')}} onBack={()=>app.setScreen('welcome')}/></AppShell>;if(app.screen==='add')return <AppShell><AddTransactionScreen entryType={app.entryType} person={app.person} amount={app.amount} dueDate={app.dueDate} saving={app.saving} note={app.note} message={app.message?.text??null} reminderEnabled={app.remindersEnabled} onReminderChange={v=>void app.setReminderPreference(v)} onEntryTypeChange={app.setEntryType} onPersonChange={app.setPerson} onAmountChange={app.setAmount} onDueDateChange={app.setDueDate} onNoteChange={app.setNote} onSave={()=>void app.handleSaveTransaction()} onBack={()=>app.setScreen(app.addReturnScreen)}/></AppShell>;if(app.screen==='calendar')return <AppShell><CalendarScreen transactions={app.transactions} onBackHome={home} onTransactions={transactions} onAdd={add} onNotifications={notifications} onProfile={profile}/></AppShell>;if(app.screen==='notifications')return <AppShell><NotificationsScreen transactions={app.transactions} onBack={home} onMarkPaid={i=>void app.handleToggleSettled(i)} onSendNudge={app.handleSendNudge} onHome={home} onTransactions={transactions} onAdd={add} onCalendar={calendar} onProfile={profile}/></AppShell>;if(app.screen==='profile')return <AppShell><ProfileScreen fullName={app.fullName} email={app.email} remindersEnabled={app.remindersEnabled} onHome={home} onTransactions={transactions} onAdd={add} onCalendar={calendar} onNotifications={notifications} onReminders={()=>app.setScreen('reminders')} onSignOut={app.handleSignOut}/></AppShell>;if(app.screen==='reminders')return <AppShell><ReminderSettingsScreen enabled={app.remindersEnabled} onEnabledChange={v=>void app.setReminderPreference(v)} onBack={profile}/></AppShell>;if(app.screen==='personDetails'&&personScreen)return <AppShell><PersonDetailsScreen person={personScreen} transactions={app.transactions} onBack={transactions} onAdd={()=>app.startAddTransaction(personScreen,'personDetails')} onToggleSettled={i=>void app.handleToggleSettled(i)}/></AppShell>;if(app.screen==='people')return <AppShell><PeopleScreen transactions={app.transactions} onBack={home} onSelectPerson={p=>{setPersonScreen(p);app.setScreen('personDetails')}} onAdd={add} onCalendar={calendar} onProfile={profile} onNotifications={notifications}/></AppShell>;return <AppShell><DashboardScreen transactions={app.transactions} userName={displayName(app.fullName,app.email)} onAdd={add} onPeople={transactions} onReminders={calendar} onSignOut={app.handleSignOut} onToggleSettled={i=>void app.handleToggleSettled(i)} onDelete={i=>void app.handleDeleteTransaction(i.id)} onNotifications={notifications} onProfile={profile}/></AppShell>}
function AppShell({children}:{children:ReactNode}){return <SafeAreaView style={styles.safeArea} edges={['top','bottom','left','right']}><StatusBar style="dark"/>{children}</SafeAreaView>} const styles=StyleSheet.create({safeArea:{flex:1,backgroundColor:'#F4F7F6'},loading:{flex:1,alignItems:'center',justifyContent:'center',gap:12},loadingText:{color:'#64748B',fontSize:14}});